// This is the postcss configuration. A tool to process CSS with different plugins. In this case, it is used to add Tailwind CSS to the project.
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
