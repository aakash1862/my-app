import React from 'react';

const Page = () => {
    // throw new Error("NOT IMPLEMENTED");
    return (
        <div>
        <h1>Routing</h1>
        <p>This is an example to show how routing configured in the app. Multiple folders means multiple routes</p>
        </div>
    );
}

export default Page;